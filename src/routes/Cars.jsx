import React, { useEffect } from 'react'

const Cars = () => {

  const handleUpdateCarName=async(car)=>{


  }

  return (
    <div>
      {/* {
        cars.map( products=>
          <div key={products.id}>
            <img src={products.images} alt="" />
            <h2>{products.title}</h2>
            <button onClick={()=> handleUpdateCarName (products)}>Update</button> 
          </div>)
      }  */}
    </div>
  )
}

export default Cars
